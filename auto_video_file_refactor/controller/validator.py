def validate(rf_struct: dict):
    # TODO: Check if the refactored structure is valid,
    #   e.g. source path exists, no duplicated output, valid destination filename;
    #   else, raise error
    pass
